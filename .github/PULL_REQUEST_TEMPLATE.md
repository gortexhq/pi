## Summary

What this PR does and why.

Closes #

## Testing

- [ ] `npm run check` passes
- [ ] New behaviour has a suite that drives it through Pi's `ExtensionRunner` (see `docs/tests.md`)
- [ ] Tried by hand in Pi (`pi -e /path/to/checkout`), if user-visible

## Checklist

- [ ] No runtime dependencies added
- [ ] README configuration table and `docs/architecture.md` updated, if behaviour or config changed
- [ ] Works on Windows paths and process spawning, if touching either
